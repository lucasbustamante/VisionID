# Correção da área técnica — VisionID 1.5

- A chave da trava de QR Code usa `androidx.appcompat.widget.SwitchCompat`.
- A preferência continua persistente via `SharedPreferences`.
- O valor padrão em uma instalação nova continua sendo `true` (trava ativada).
- A versão exibida permanece 1.5; o `versionCode` foi atualizado para 12.
