<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- O NSDK inclui localizacao para modulos Bluetooth nao usados pelo app. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />

    <!-- Fallback da impressora interna XCheng exposta como BluetoothPrinter. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <!-- Permite localizar o serviço interno da impressora da família Positivo/XCheng. -->
    <queries>
        <package android:name="com.xcheng.printerservice" />
        <intent>
            <action android:name="com.xcheng.printerservice.IPrinterService" />
        </intent>
    </queries>

    <application
        android:name=".VisionIdApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="${cleartextTrafficPermitted}"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:label="Bio Facial"
        android:theme="@style/Theme.LaranjinhaQrWebView">

        <activity
            android:name=".CameraTestActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".TechnicalAreaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".LogDetailActivity"
            android:exported="false" />

        <activity
            android:name=".LogsActivity"
            android:exported="false" />


        <activity
            android:name=".GeckoBrowserActivity"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:process=":gecko"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateUnspecified|adjustResize" />

        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:process=":webcontent" />

        <activity
            android:name=".QrScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
